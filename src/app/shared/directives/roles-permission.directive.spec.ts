import { RolesPermissionDirective } from './roles-permission.directive';

describe('RolesPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
